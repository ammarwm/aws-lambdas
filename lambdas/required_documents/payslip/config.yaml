region: ap-southeast-2
function_name: RequiredDocumentsPayslip
handler: required_payslip_function.handler
role: LendiAIAPI
description: (Smart Documents) It is called to get the required documents (payslips)

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
timeout: 30
memory_size: 128
